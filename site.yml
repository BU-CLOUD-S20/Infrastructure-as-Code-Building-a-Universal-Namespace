---
# This playbook will set up Upspin server on OpenStack platform

- hosts: localhost
  connection: local
  remote_user: ubuntu
  gather_facts: false

  tasks:

    - import_role:
        name: openstack
    - import_role:
        name: upspin